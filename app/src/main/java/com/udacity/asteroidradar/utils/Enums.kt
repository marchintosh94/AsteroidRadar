package com.udacity.asteroidradar.utils

object Enums {
    enum class LoaderStatus {
        LOADING,
        DONE
    }

    enum class AsteroidFilter {
        WEEK,
        TODAY,
        ALL
    }
}